



export type Mesh = {
    faces: number[],
    vertices: number[],
    vertices_size: number,
    colours: number[] | null,
    colour_size: number | null,
    normals: number[] | null,
    normal_size: number | null,
    uvs: number[] | null,
    uv_size: number | null
}